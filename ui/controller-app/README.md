# Вокзал.ТЕХ - Controller App (Приложение контроллёра)

PWA (Progressive Web App) для контроллёров: проверка билетов через сканирование QR-кодов, отметка посадки пассажиров.

## Функции

- 📱 **PWA**: устанавливается как приложение на смартфон
- 🔐 **Авторизация**: вход для контроллёров
- 📋 **Выбор рейса**: список активных рейсов в статусе "Посадка"
- 📷 **QR-сканер**: камера для сканирования билетов
- ✅ **Отметка посадки**: автоматическая проверка и регистрация
- 📊 **Статистика**: прогресс посадки в реальном времени
- 🔊 **Звуковые сигналы**: успешная/неуспешная проверка
- 📳 **Вибрация**: тактильная обратная связь
- 🌐 **Offline-ready**: работа без интернета (Service Worker)

## Технологии

- **React 19.2.4+** - UI библиотека
- **TypeScript 5.9.3+** - типизация
- **Vite 7.3.1+** - сборщик
- **Vite PWA Plugin 0.21.4+** - превращение в PWA
- **Fluent UI React 9.72.9+** - компоненты Microsoft
- **React Router 7.12.0+** - маршрутизация
- **TanStack Query 5.90.19+** - работа с API
- **Zustand 5.0.10+** - state management
- **Axios 1.13.2+** - HTTP клиент
- **html5-qrcode 2.3.8+** - QR-сканер
- **date-fns 4.1.0+** - работа с датами
- **Zod 4.3.5+** - валидация форм

## Установка

```bash
# Из корня монорепозитория
cd ui/controller-app

# Установка зависимостей
npm install

# Запуск dev-сервера (порт 3003)
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг
npm run lint
```

## Конфигурация

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Переменные окружения:

- `VITE_API_URL` - URL бэкенда API (по умолчанию `/api/v1`)
- `VITE_ENABLE_OFFLINE` - включение offline-режима (по умолчанию `true`)
- `VITE_ENABLE_CAMERA` - включение камеры (по умолчанию `true`)

## Структура

```
controller-app/
├── src/
│   ├── components/           # Переиспользуемые компоненты
│   │   ├── ProtectedRoute.tsx   # Защита роутов
│   │   └── QRScanner.tsx        # QR-сканер (html5-qrcode)
│   ├── pages/                # Страницы
│   │   ├── LoginPage.tsx          # Вход
│   │   ├── TripSelectionPage.tsx  # Выбор рейса
│   │   └── ScanPage.tsx           # Сканирование QR
│   ├── services/             # API клиенты
│   │   ├── api.ts         # Axios instance
│   │   ├── auth.ts        # Авторизация
│   │   ├── trip.ts        # Рейсы
│   │   └── ticket.ts      # Билеты
│   ├── stores/               # Zustand stores
│   │   ├── authStore.ts      # Аутентификация
│   │   └── scanStore.ts      # Сканирование
│   ├── types/                # TypeScript типы
│   │   └── index.ts
│   ├── utils/                # Утилиты
│   │   └── format.ts         # Форматирование, звуки, вибрация
│   ├── App.tsx               # Главный компонент
│   ├── main.tsx              # Точка входа + PWA регистрация
│   └── index.css             # Глобальные стили
├── public/                   # Статические ресурсы
│   ├── pwa-192x192.png       # PWA иконка 192x192
│   └── pwa-512x512.png       # PWA иконка 512x512
├── index.html                # HTML шаблон
├── vite.config.ts            # Конфигурация Vite + PWA
├── tsconfig.json             # Конфигурация TypeScript
└── package.json              # Зависимости
```

## Основные сценарии

### 1. Вход и выбор рейса

1. Открыть приложение (веб или PWA)
2. Войти как контроллёр (логин/пароль)
3. Выбрать рейс из списка активных (статус "Посадка")
4. Перейти к сканированию

### 2. Проверка билета

1. Камера автоматически включается
2. Навести на QR-код билета
3. Система проверяет билет:
   - ✅ Валидный билет → звуковой сигнал + вибрация + зелёная карточка
   - ❌ Невалидный билет → ошибочный сигнал + вибрация + красная карточка
4. Результат отображается 3 секунды, затем автоматически возобновляется сканирование

### 3. Мониторинг прогресса

- **Статистика рейса**: продано / прошли посадку / прогресс (%)
- **Недавние проверки**: последние 5 пассажиров с временем посадки
- **Автообновление**: статистика обновляется каждые 10 секунд

## PWA (Progressive Web App)

### Установка на устройство

#### Android (Chrome, Edge)
1. Открыть приложение в браузере
2. Нажать "Меню" (⋮) → "Установить приложение" или "Добавить на главный экран"
3. Подтвердить установку
4. Иконка появится на главном экране

#### iOS (Safari)
1. Открыть приложение в Safari
2. Нажать "Поделиться" (квадрат со стрелкой вверх)
3. Выбрать "На экран «Домой»"
4. Подтвердить

### Возможности PWA

- ✅ **Работает как нативное приложение** (полноэкранный режим, иконка на главном экране)
- ✅ **Offline-режим** (Service Worker кэширует запросы)
- ✅ **Обновления** (автоматическое обновление при выходе новой версии)
- ✅ **Доступ к камере** (для QR-сканера)
- ✅ **Push-уведомления** (опционально, для критичных событий)

## API Endpoints (используются)

- `POST /v1/auth/login` - вход
- `POST /v1/auth/refresh` - обновление токена
- `GET /v1/trips?status=boarding` - активные рейсы
- `GET /v1/trips/:id` - детали рейса
- `GET /v1/trips/:id/stats` - статистика рейса
- `GET /v1/tickets/by-qr?qr=:qrCode` - получение билета по QR
- `POST /v1/tickets/boarding` - отметка посадки
- `GET /v1/tickets?tripId=:tripId` - билеты рейса

## Соответствие требованиям

- **152-ФЗ**: аудит посадки (запись в audit_logs через backend)
- **Безопасность**: JWT авторизация, RBAC (только контроллёры)
- **Брендинг**: цвета Вокзал.ТЕХ, кириллица, Russian-first
- **UX**: звуковые сигналы, вибрация, автовозобновление сканирования

## Тестирование

```bash
# Unit тесты (Jest)
npm test

# E2E тесты (Cypress)
npm run test:e2e

# Тестирование PWA
npm run build
npm run preview
# Открыть в Chrome DevTools → Application → Service Workers
```

## Развертывание

```bash
# Production build
npm run build

# Результат в dist/
# Включает:
# - index.html
# - assets/ (JS, CSS)
# - manifest.webmanifest (PWA манифест)
# - sw.js (Service Worker)

# Деплой через Traefik + Nginx/Caddy с HTTPS (требуется для PWA)
```

## Требования к устройствам

### Минимальные требования
- Android 5.0+ / iOS 11.3+
- Chrome 67+, Safari 11.1+, Edge 79+
- Камера с автофокусом (для QR-сканера)
- HTTPS (для PWA и доступа к камере)

### Рекомендуемые устройства
- Смартфоны с процессором от Snapdragon 625 / A10 Bionic
- 2 ГБ RAM+
- Камера 8 Мп+
- 4G/Wi-Fi

## Отладка

### QR-сканер не работает
- Проверьте, что приложение открыто по HTTPS (или localhost)
- Разрешите доступ к камере в браузере
- Убедитесь, что камера работает (проверьте в другом приложении)

### PWA не устанавливается
- Убедитесь, что приложение обслуживается по HTTPS
- Проверьте наличие manifest.webmanifest и иконок
- Проверьте консоль браузера на ошибки Service Worker

### Проблемы с offline-режимом
- Откройте Chrome DevTools → Application → Service Workers
- Проверьте статус Service Worker (должен быть "activated")
- Проверьте Cache Storage на наличие кэшированных ресурсов

## Лицензия

Проприетарное ПО © 2026 Вокзал.ТЕХ
