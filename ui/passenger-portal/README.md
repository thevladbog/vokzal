# Вокзал.ТЕХ - Passenger Portal (Портал пассажира)

Веб-приложение для пассажиров: поиск рейсов, покупка билетов, электронный билет с QR-кодом.

## Функции

- 🔍 **Поиск рейсов**: по станциям, дате, времени
- 🎫 **Покупка билетов**: бронирование мест, ввод данных пассажиров
- 💳 **Оплата**: карты (Tinkoff), СБП (QR-код), наличные в кассе
- 📱 **Электронный билет**: QR-код для посадки
- 📋 **Мои билеты**: история покупок, возврат билетов
- 🔐 **Регистрация/Вход**: личный кабинет

## Технологии

- **React 19.2.4+** - UI библиотека
- **TypeScript 5.9.3+** - типизация
- **Vite 7.3.1+** - сборщик
- **Fluent UI React 9.72.9+** - компоненты Microsoft
- **React Router 7.12.0+** - маршрутизация
- **TanStack Query 5.90.19+** - работа с API
- **Zustand 5.0.10+** - state management
- **Axios 1.13.2+** - HTTP клиент
- **date-fns 4.1.0+** - работа с датами
- **qrcode.react 4.1.0+** - генерация QR-кодов
- **Zod 4.3.5+** - валидация форм

## Установка

```bash
# Из корня монорепозитория
cd ui/passenger-portal

# Установка зависимостей
npm install

# Запуск dev-сервера (порт 3002)
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг
npm run lint
```

## Конфигурация

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Переменные окружения:

- `VITE_API_URL` - URL бэкенда API (по умолчанию `/api/v1`)
- `VITE_ENABLE_REGISTRATION` - включение регистрации (по умолчанию `true`)
- `VITE_ENABLE_SBP` - включение СБП оплаты (по умолчанию `true`)

## Структура

```
passenger-portal/
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   │   ├── ProtectedRoute.tsx  # Защита роутов
│   │   └── TripCard.tsx        # Карточка рейса
│   ├── pages/           # Страницы
│   │   ├── HomePage.tsx          # Поиск рейсов
│   │   ├── LoginPage.tsx         # Вход
│   │   ├── RegisterPage.tsx      # Регистрация
│   │   ├── BookingPage.tsx       # Бронирование
│   │   ├── PaymentPage.tsx       # Оплата
│   │   ├── ConfirmationPage.tsx  # Электронный билет
│   │   └── MyTicketsPage.tsx     # Мои билеты
│   ├── services/        # API клиенты
│   │   ├── api.ts        # Axios instance
│   │   ├── auth.ts       # Авторизация
│   │   ├── station.ts    # Станции
│   │   ├── trip.ts       # Рейсы
│   │   ├── ticket.ts     # Билеты
│   │   └── payment.ts    # Оплата
│   ├── stores/          # Zustand stores
│   │   ├── authStore.ts     # Аутентификация
│   │   ├── searchStore.ts   # Параметры поиска
│   │   └── bookingStore.ts  # Бронирование
│   ├── types/           # TypeScript типы
│   │   └── index.ts
│   ├── utils/           # Утилиты
│   │   └── format.ts    # Форматирование данных
│   ├── App.tsx          # Главный компонент
│   ├── main.tsx         # Точка входа
│   └── index.css        # Глобальные стили
├── index.html           # HTML шаблон
├── vite.config.ts       # Конфигурация Vite
├── tsconfig.json        # Конфигурация TypeScript
└── package.json         # Зависимости
```

## Основные сценарии

### 1. Поиск и покупка билета (гость)

1. Открыть главную страницу `/`
2. Выбрать станции отправления/прибытия, дату
3. Нажать "Найти рейсы"
4. Выбрать рейс из списка
5. Заполнить данные пассажира (ФИО, телефон, документ)
6. Выбрать способ оплаты (карта/СБП)
7. Оплатить
8. Получить электронный билет с QR-кодом

### 2. Покупка билета (зарегистрированный пользователь)

1. Войти в личный кабинет `/login`
2. Аналогично сценарию 1, но данные пассажира автозаполняются
3. После покупки билет сохраняется в "Мои билеты"

### 3. Просмотр и возврат билета

1. Войти в личный кабинет `/login`
2. Открыть "Мои билеты" `/my-tickets`
3. Просмотреть историю покупок
4. Запросить возврат билета (до отправления рейса)

## API Endpoints (используются)

- `POST /v1/auth/login` - вход
- `POST /v1/auth/register` - регистрация
- `POST /v1/auth/refresh` - обновление токена
- `GET /v1/stations` - список станций
- `POST /v1/trips/search` - поиск рейсов
- `GET /v1/trips/:id` - детали рейса
- `POST /v1/tickets` - создание билета
- `GET /v1/tickets/:id` - детали билета
- `GET /v1/tickets/user/:userId` - билеты пользователя
- `PUT /v1/tickets/:id/refund` - возврат билета
- `POST /v1/payments` - создание платежа
- `GET /v1/payments/:id` - статус платежа

## Соответствие требованиям

- **152-ФЗ**: шифрование персональных данных (AES-256), согласие на обработку ПД
- **54-ФЗ**: передача данных для фискализации в Fiscal Service
- **Брендинг**: использование цветов Вокзал.ТЕХ, кириллица, Russian-first
- **Доступность**: адаптивный дизайн, поддержка клавиатуры, ARIA-метки

## Тестирование

```bash
# Unit тесты (Jest)
npm test

# Coverage
npm run test:coverage
```

## Развертывание

```bash
# Production build
npm run build

# Результат в dist/
# Деплой через Traefik + Nginx/Caddy
```

## Лицензия

Проприетарное ПО © 2026 Вокзал.ТЕХ
