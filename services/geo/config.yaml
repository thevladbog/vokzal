server:
  port: "8090"
  mode: "debug"

redis:
  host: "localhost"
  port: 6379
  password: "${VOKZAL_REDIS_PASSWORD}"
  db: 0

logger:
  level: "debug"

yandex_maps:
  api_key: "${VOKZAL_YANDEX_MAPS_API_KEY}"
  base_url: "https://geocode-maps.yandex.ru/1.x/"
